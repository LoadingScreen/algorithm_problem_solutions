def single_number(nums)
    snums = nums.sort
    i = 0
    while i < snums.size
        return snums[i] if (snums[i] != snums[i+1])
        i += 2
    end
end

#time complexity O(n)
#space complexity O(n)

# https://leetcode.com/problems/array-partition-i/#/description

# @param {Integer[]} nums
# @return {Integer}
def array_pair_sum(nums)
    snums = nums.sort
    sum = 0
    nums.each_with_index { |a,i| sum+=a if i % 2 == 0 }
    sum
    
end
